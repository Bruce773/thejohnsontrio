import styled from 'styled-components';
import { Paper } from '@material-ui/core';

export const NavWrapper = styled(Paper)`
  width: 100%;
  font-size; 20px;
  padding: 10px;
  z-index: 9999;
`;

export const NavItem = styled.div`
  font-size: 20px;
  display: inline;
`;
